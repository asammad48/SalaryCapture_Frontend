import { ParseAbstractControlToFormGroupPipe } from './parse-abstract-control-to-form-group.pipe';

describe('ParseAbstractControlToFormGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseAbstractControlToFormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
